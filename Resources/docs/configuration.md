Configuration
=============

Configure connections to supervisor instances:

```yaml
abc_supervisor:
    connections:
        localhost:
            host: localhost
            port: 9001
            username: user
            password: secret
```

Next Step: [Basic Usage](./basic-usage.md)